import "./App.css";
import StartPage from "./components/Common/StartPage.tsx";

/**
 * The main App component that serves as the entry point for the application.
 * Renders the StartPage component.
 *
 * @returns {JSX.Element} The rendered StartPage component.
 */
function App() {
    return <StartPage />;
}

export default App;
